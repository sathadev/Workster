<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login - WorkSter</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #fff;
    }
    .login-container {
      width: 100%;
      max-width: 360px;
      text-align: center;
    }
    .login-logo {
      width: 120px;
      margin-bottom: 30px;
    }
    .form-control {
      border-radius: 10px;
      margin-bottom: 15px;
    }
    .btn-login {
      background-color: #1E56A0;
      color: white;
      border-radius: 10px;
      width: 100%;
    }
    .btn-login:hover {
      background-color: #174a8c;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <img src="/images/logo.png" alt="WorkSter Logo" class="login-logo" />
    <form method="POST" action="/login">
      <input type="text" class="form-control" name="emp_username" placeholder="Username" required />
      <input type="password" class="form-control" name="emp_password" placeholder="Password" required />
      <% if(error){ %>
        <div class="alert alert-danger"><%= error %></div>
      <% } %>
      <button type="submit" class="btn btn-login">Login</button>
    </form>
  </div>
</body>
</html>
